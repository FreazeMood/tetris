class Board {
    
    constructor({ cell_size, rows, columns }) {
        this.cell_size = cell_size;
        this.rows = rows;
        this.columns = columns;
    }
    
    
    init() {
        // initialize the board on the document. 
    }
}